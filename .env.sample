# Postgres (infra service, services/postgres/compose.yml).
# These are local-only defaults — safe because Postgres is never
# host-published, only reachable on the internal homelab-net network.
# Override in .env (gitignored) if you need different credentials.
POSTGRES_USER=homelab
POSTGRES_PASSWORD=homelab
POSTGRES_DB=homelab
