networks:
  homelab-net:
    name: homelab-net

include:
  - services/postgres/compose.yml
  - services/nginx/compose.yml
  - services/redis/compose.yml

services:
  homelab:
    build: .
    container_name: homelab
    networks: [homelab-net]
    env_file:
      - .env
    ports:
      - "5173:5173"
      - "55123:55123"
      - "3000:3000"
    volumes:
      - ${WORKSPACE:-./volume}:/root/workspace
      - ./volume/infra/claude:/root/.claude
      - ${DOCKER_SOCK_PATH:-/dev/null}:/var/run/docker.sock:ro
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:55123/healthz"]
      interval: 30s
      timeout: 3s
      retries: 3
      start_period: 5s
    stdin_open: true
    tty: true
    restart: unless-stopped
